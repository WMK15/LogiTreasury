/**
 * Wagmi Configuration Export
 * Re-exports config for service layer
 */

export { config, arcTestnet, CONTRACTS, DECIMALS, formatUSDC, formatEUR, formatPercent, shortenAddress } from './config';
